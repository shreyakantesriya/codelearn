// it is new featuer of ECMADcript 5
// the statement "use strict": instructs the browser to use the scrit mode ,which is a reduced and safer feacter set of javascript
"use strict"
function sum(a,b){

    console.log(add);
   var add = a+b;
    console.log(add);
}
sum(10,5)
